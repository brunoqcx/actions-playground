name: notifier
on:
  pull_request:
    types: [labeled]
jobs:
  build:
    if: github.event.label.name == 'notify'
    name: notificar
    runs-on: ubuntu-latest
    steps:
      - name: notify
        env:
          MY_NAME: mayke
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
